# ExpenseTracker (Angular Ionic + Capacitor)

## Dev
```bash
nvm use 22
npm install
npm start
```

## Android APK
```bash
npm run build
npx cap sync android
cd android && ./gradlew assembleRelease
```

Requires **Java 21** and Android SDK.
